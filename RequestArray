for requests page, use double array to store values and user, then have foreach for each section (div) to sort and display users
EX: foreach user in user's.array, string n = user.userID, if user = [0,n], then run div/create display for user
first value of array is type (0,1,or 2), second value is userID
